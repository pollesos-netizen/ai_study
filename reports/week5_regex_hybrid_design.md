# 5주차 정규식 + AI 하이브리드 탐지 설계 결과

## 1. 목적

4주차까지 진행한 AI 문장분류 모델은 성명, 건강정보, 복지정보, 계약정보 등 문맥형 민감정보를 탐지하는 데 활용 가능성을 보였다.

다만 이메일, 주민등록번호, 내부 IP, VLAN, 포트 등 형태가 명확한 패턴형 정보는 AI 모델보다 정규식 기반 탐지가 더 안정적이다.

따라서 5주차에서는 다음 구조를 설계하였다.

```text
정규식 탐지
+ AI 문장분류
+ 규칙 기반 C/S/O 등급 산정
= 하이브리드 개인정보/민감정보 탐지 구조
```

---

## 2. 역할 분담

| 구분 | 역할 | 주요 대상 |
|---|---|---|
| 정규식 탐지 | 형태가 명확한 패턴형 정보 탐지 | 주민등록번호, 여권번호, 이메일, 전화번호, 사번, 내부 IP, VLAN, 포트, 주파수, 키로정, 정밀 시각 |
| AI 문장분류 | 문맥 판단이 필요한 정보 탐지 | 성명, 건강정보, 복지정보, 인사·징계, 계약·입찰·평가자료 |
| 규칙 엔진 | 정규식 결과와 AI 결과를 결합해 최종 C/S/O 등급 산정 | 문장 단위 최종 등급 결정 |

---

## 3. 정규식 우선 탐지 대상

| 항목 | 예시 | 등급 | 권장 조치 | 비고 |
|---|---|---|---|---|
| 주민등록번호 | 900101-1234567 | C | 삭제 | 고유식별정보 |
| 여권번호 | M12345678 | C | 삭제 | 고유식별정보 |
| 내부 IP 주소 | 192.168.0.1 | C | 삭제 | 내부 네트워크 정보 |
| VLAN/포트 정보 | VLAN 100, port 8080 | C | 삭제 | 내부 네트워크 구성 정보 |
| 전화번호 | 010-1234-5678 | S | 마스킹 | 직접 식별 가능한 개인정보 |
| 이메일 주소 | test@example.com | S | 마스킹 | 직접 식별 가능한 개인정보 |
| 사번 | CD789012 | S | 마스킹 | 개인 식별 가능 정보 |
| 계좌번호 | 110-123-456789 | S | 마스킹 | 금융정보 |
| 무선 주파수 | 75.450MHz | S | 치환 | 통신 보안 정보 |
| 정밀 위치 | 37K500 | S | 범주화 | 위치정보 |
| 정밀 시각 | 02:05:30 | S | 범주화 | 보안 공백과 결합 시 민감 |

---

## 4. 한국인 성명 추정 제외

한국인 성명은 정규식 탐지 대상에서 제외하였다.

이유는 다음과 같다.

```text
1. 성명은 문맥 없이 판단하기 어렵다.
2. 정규식 기반 성명 추정은 오탐 가능성이 높다.
3. 성명은 향후 AI 문장분류 또는 NER 모델로 다루는 것이 적절하다.
```

예시:

```text
최지연 씨의 서류가 접수되었습니다.
```

위 문장은 정규식 탐지 결과는 없지만, AI 문장분류 모델이 개인정보로 판단할 수 있다.

---

## 5. Python 정규식 처리 시 수정한 점

기존 JSX 정규식에서는 `\b`를 사용해도 한글 조사와 붙은 값이 정상 탐지되었다.

하지만 Python 정규식에서는 한글이 Unicode 단어 문자로 처리되어 `\b`가 예상대로 동작하지 않을 수 있었다.

예시:

```text
900101-1234567이
test@example.com입니다
75.450MHz가
02:05:30부터
```

따라서 Python 구현에서는 `\b` 대신 숫자/영문 기준 경계 조건을 직접 사용하였다.

예시:

```python
r"(?<!\d)\d{6}[-–]\d{7}(?!\d)"
```

의미:

```text
앞뒤에 숫자가 이어지지 않는 6자리-7자리 숫자 패턴 탐지
```

이 방식은 한글 조사와 붙은 값도 안정적으로 탐지한다.

---

## 6. 구현 파일

5주차에서 구현한 주요 파일은 다음과 같다.

```text
src/
  regex_detector.py
  hybrid_detector_v2.py
```

### regex_detector.py

정규식 기반 패턴 탐지기이다.

주요 함수:

| 함수 | 역할 |
|---|---|
| `detect_patterns(text)` | 입력 문장에서 정규식 패턴을 탐지 |
| `get_max_grade(results)` | 탐지된 정규식 결과 중 최고 등급 반환 |
| `print_detection_result(text)` | 정규식 탐지 결과 출력 |

### hybrid_detector_v2.py

정규식 탐지 결과와 AI 모델 예측 결과를 결합한다.

주요 함수:

| 함수 | 역할 |
|---|---|
| `load_ai_model(model_path)` | 4주차에서 저장한 AI 모델 불러오기 |
| `hybrid_classify(text, ai_model)` | 정규식 탐지 + AI 예측 + 최종 등급 산정 |
| `decide_final_grade(regex_grade, ai_grade)` | 문장 단위 최종 등급 결정 |
| `select_highest_grade(grades)` | 문서 전체 등급 산정 시 사용할 최고 등급 선택 |
| `print_hybrid_result(result)` | 하이브리드 판단 결과 출력 |

---

## 7. 최종 등급 산정 원칙

한 문장에 대한 최종 등급은 다음 원칙으로 결정한다.

```text
1. 정규식 탐지 결과가 있으면 정규식 등급을 우선 적용한다.
2. 정규식 탐지 결과가 없을 때만 AI 문장분류 결과를 적용한다.
3. AI 예측은 최종 등급과 별개로 참고 근거로 함께 보관한다.
```

이유는 다음과 같다.

```text
정규식 탐지 대상은 예외가 거의 없는 명확한 패턴형 정보이므로,
AI보다 정규식 결과를 더 신뢰하는 것이 적절하다.
```

---

## 8. 정규식 탐지 없음과 O 등급 구분

초기 구현에서는 정규식 탐지가 없을 때 `O`로 표시되었다.

하지만 이는 의미상 부정확하다.

```text
정규식 탐지 없음 ≠ O 등급
```

따라서 수정 후에는 다음처럼 구분한다.

| 상태 | 의미 |
|---|---|
| 정규식 기준 등급: 해당 없음 | 정규식 탐지 대상이 발견되지 않음 |
| 최종 C/S/O 등급: O | 정규식 탐지 없음 + AI도 일반으로 판단 |
| 최종 C/S/O 등급: S | 정규식 탐지 없음 + AI가 개인정보 또는 민감정보로 판단 |
| 최종 C/S/O 등급: C/S | 정규식 탐지 결과가 있어 해당 등급 우선 적용 |

수정된 핵심 로직:

```python
def get_regex_grade_or_none(regex_results):
    if not regex_results:
        return None

    return get_max_grade(regex_results)
```

```python
def decide_final_grade(regex_grade, ai_grade):
    if regex_grade is not None:
        return regex_grade, "정규식 탐지 결과 우선 적용"

    return ai_grade, "정규식 탐지 없음 — AI 문장분류 결과 적용"
```

---

## 9. 하이브리드 탐지 테스트 결과

| 문장 | AI 예측 | 정규식 기준 등급 | 최종 등급 | 판단 근거 |
|---|---|---|---|---|
| 회의 결과를 요약하여 공유드립니다. | 민감정보 → S | 해당 없음 | S | 정규식 탐지 없음 — AI 문장분류 결과 적용 |
| 최지연 씨의 서류가 접수되었습니다. | 개인정보 → S | 해당 없음 | S | 정규식 탐지 없음 — AI 문장분류 결과 적용 |
| test@example.com으로 자료를 보내드리겠습니다. | 개인정보 → S | S | S | 정규식 탐지 결과 우선 적용 |
| 주민등록번호 900101-1234567이 포함되어 있습니다. | 민감정보 → S | C | C | 정규식 탐지 결과 우선 적용 |
| 서버 IP는 192.168.0.1이고 VLAN 100, port 8080을 사용합니다. | 일반 → O | C | C | 정규식 탐지 결과 우선 적용 |
| 진단서가 접수되었습니다. | 민감정보 → S | 해당 없음 | S | 정규식 탐지 없음 — AI 문장분류 결과 적용 |
| 장애인 등록 신청서가 접수되었습니다. | 민감정보 → S | 해당 없음 | S | 정규식 탐지 없음 — AI 문장분류 결과 적용 |
| 입찰 제안 평가표를 검토했습니다. | 민감정보 → S | 해당 없음 | S | 정규식 탐지 없음 — AI 문장분류 결과 적용 |
| 무선설비 주파수 75.450MHz가 기재되어 있습니다. | 민감정보 → S | S | S | 정규식 탐지 결과 우선 적용 |
| 보안 패치 적용은 02:05:30부터 시작됩니다. | 일반 → O | S | S | 정규식 탐지 결과 우선 적용 |

---

## 10. 테스트 결과 해석

### 정상 동작 사례

#### 주민등록번호

```text
AI 예측: 민감정보 → S
정규식 기준 등급: C
최종 등급: C
```

AI는 C/S/O 세부 기준을 직접 판단하지 못했지만, 정규식이 주민등록번호를 탐지하여 최종 C로 산정하였다.

#### 내부 IP, VLAN, 포트

```text
AI 예측: 일반 → O
정규식 기준 등급: C
최종 등급: C
```

AI가 일반으로 오판했지만, 정규식이 C급 네트워크 정보를 탐지하여 최종 C로 보정하였다.

#### 진단서, 장애인 등록, 입찰 평가표

```text
정규식 기준 등급: 해당 없음
AI 예측: 민감정보 → S
최종 등급: S
```

정규식으로 잡기 어려운 문맥형 민감정보를 AI가 보완하였다.

---

## 11. 현재 남은 이슈

### AI 모델 오탐

다음 문장은 일반 업무 문장에 가까우나, AI가 민감정보로 예측하였다.

```text
회의 결과를 요약하여 공유드립니다.
```

현재 하이브리드 구조에서는 정규식 탐지가 없기 때문에 AI 판단을 적용하여 최종 S가 된다.

이는 코드 로직의 문제라기보다 AI 모델의 오탐 문제이다.

개선 방향:

```text
1. 일반 업무 공유 문장 데이터 추가
2. AI 예측 확률 확인
3. 확신도가 낮은 경우 '검토 필요' 상태로 분리
```

### 건강정보의 C급 상향 필요성

현재 AI가 `민감정보`로 예측하면 S로 변환한다.

그러나 가이드라인 기준으로 건강정보, 병력, 진단서 등 법령상 민감정보는 C급 후보로 관리해야 할 수 있다.

향후 개선 방향:

```text
AI 예측: 민감정보
+ 건강정보 키워드 또는 category 확인
→ C 후보 또는 C로 상향 검토
```

---

## 12. 현재 구조의 의미

현재 하이브리드 구조는 다음 역할 분담을 구현하였다.

```text
정규식:
명확한 패턴형 정보 탐지

AI:
문맥형 개인정보/민감정보 판단

규칙 엔진:
정규식 결과와 AI 결과를 결합해 최종 C/S/O 등급 산정
```

즉, 실제 개인정보/민감정보 탐지기의 기본 아키텍처는 다음과 같다.

```text
Regex Detector
+ ML Text Classifier
+ Rule-based Grade Engine
= Hybrid Privacy/Sensitive Data Detector
```

---

## 13. 다음 개선 방향

5주차 이후에는 다음 내용을 검토한다.

```text
1. 일반 업무 공유 문장 데이터 보완
2. AI 예측 확률 기반 검토 필요 로직 추가
3. 건강정보·고유식별정보·네트워크정보의 C급 상향 규칙 세분화
4. category별 성능 분석
5. 문장 단위 등급을 모아 문서 전체 등급 산정
6. 비식별화 조치 방식 자동 추천 구조 설계
7. TensorFlow/Keras 재구현은 라벨 구조 정리 이후 진행
```

---

## 14. 5주차 결론

5주차 목표였던 정규식 + AI 하이브리드 탐지 구조를 구현하였다.

핵심 성과는 다음과 같다.

```text
1. 정규식 패턴형 정보 탐지 구현
2. Python 정규식의 한글 조사 문제 해결
3. 한국인 성명 추정은 정규식에서 제외
4. 정규식 탐지 없음과 O 등급 구분
5. 정규식 탐지 결과 우선 적용 로직 구현
6. AI 문장분류 모델과 결합
7. 최종 C/S/O 등급 산정 구조 확인
```

현재 구조는 완성형은 아니지만, 개인정보/민감정보 탐지기 고도화를 위한 기본 설계로 활용 가능하다.
