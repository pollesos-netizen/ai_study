# 비식별화 도구 의존성

# === 파일 처리 ===
python-docx>=1.1.0          # docx
python-pptx>=1.0.0          # pptx
openpyxl>=3.1.0             # xlsx
pdfplumber>=0.11.0          # pdf
lxml>=5.0.0                 # hwpx (XML 파싱)

# === HTTP API ===
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20    # 파일 업로드
pydantic>=2.10.0

# === 환경변수 ===
python-dotenv>=1.0.0        # .env 파일 로드

# === AI 모델 (sklearn — 기본) ===
scikit-learn>=1.3.0
joblib>=1.3.0

# === AI 모델 (NER — useNer=true 사용 시) ===
# transformers>=4.45.0
# torch>=2.5.0

# === AI 모델 (Keras — AI_MODEL_TYPE=keras 사용 시) ===
# tensorflow>=2.18.0

# 설치 실패 시 (사내망 SSL 이슈):
#   스마트폰 핫스팟으로 wheel 다운로드 후 pip install <wheel파일>
